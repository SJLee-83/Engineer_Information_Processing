#include <stdio.h>
#include <stdlib.h> // 1?

main(int argc, char *argv[]) { // 2?
    int v1 = 0;
    int v2 = 35;
    int v3 = 29;
    if (v1 > v2 ? v2 : v1) // v1이 v2보다 크면 v2의 값을 조건으로 사용하고 그렇지 않으면 v1의 값을 같은 조건으로 사용
        v2 = v2 << 2; // v1 = 0, v2 = 35 v1 > v2는 거짓이라 해당 조건문 pass
    else
        v3 = v3 << 2; // << : 시프트 연산자, v3에 지정된 값을 2비트 이동시킨 후, v3에 저장 29 = 11101 -> 1110100 = 4 + 16 + 32 + 64 = 116
    printf("%d", v2 + v3); // 116 + 35 = 151
    return 0;
}

// stdlib.h는 C언어의 표준 라이브러리(Standard Library) 헤더 파일로, 메모리 할당(malloc, free), 난수 생성(rand), 문자열 변환, 프로그램 종료(exit) 등 필수적인 함수들을 포함하고 있습니다. 
// 이 특정 코드에서는 stdlib.h의 함수를 사용하지 않아 없어도 정상적으로 컴파일되고 실행됩니다.


// if (v1 > v2 ? v2 : v1)
// 이 코드는 **삼항 연산자(Ternary Operator)**를 if문 조건으로 사용한 것입니다.

// 삼항 연산자 문법: (조건) ? (참일 때의 값) : (거짓일 때의 값)

// 코드의 v1 > v2 ? v2 : v1를 해석하면 다음과 같습니다.

// "만약 v1이 v2보다 크면(참이면) v2를 선택하고, 그렇지 않으면(거짓이면) v1을 선택하라."

// 실제 값으로 보면 0 > 35는 거짓이므로, 이 연산의 최종 결과는 v1의 값인 0이 됩니다. 따라서 if문은 if (0)이 되고, else 블록으로 넘어가게 됩니다.


// <<는 **왼쪽 시프트 연산자(Left Shift Operator)**입니다. Shifts the bits of a number to the left.

// 동작 방식: 숫자를 2진수로 바꾼 뒤, 모든 비트를 왼쪽으로 지정된 횟수만큼 밀고 오른쪽 빈자리는 0으로 채우는 연산입니다. ⚙️

// 수학적 의미: 왼쪽으로 1번 시프트하는 것은 2를 곱하는 것과 같습니다. 왼쪽으로 N번 시프트하는 것은 2^N을 곱하는 것과 같습니다.

// 코드의 v3 << 2는 v3를 왼쪽으로 2번 시프트하라는 의미이므로, v3 * 2^2, 즉 **v3 * 4**와 동일한 연산입니다.